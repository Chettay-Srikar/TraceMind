# GitHub Push Rule

After every task is successfully completed, you MUST automatically push the changes to GitHub.

To do this:
1. Stage all changes: `git add .`
2. Commit the changes: `git commit -m "Auto-commit after task completion"` (use a more descriptive message if appropriate)
3. Push to the repository: `git push`
